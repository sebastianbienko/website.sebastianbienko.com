export interface TileProps {
    title: string,
    subtitle: string,
    icon: any,
    skills: string[]
}

export interface TilePositioningProps {
    top?: boolean,
    right?: boolean,
    bottom?: boolean,
    left?: boolean
}